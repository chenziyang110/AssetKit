## 项目资产协议

涉及文档、图片、视频、音频、模型、数据集、设计稿或模板的查找、创建、下载、引用、修改或归档时，先读取 `{{SKILL_PATH}}/SKILL.md` 并按其中路由读取必要手册。
创建前先检索；正式保留的资源通过 CLI 登记；元数据修改用 revision 字段补丁，不读取或重写全量账本。
项目账本位于根目录 `.assets/`，不存进 Skill 目录。未知来源/许可保留 candidate；没有成功回执或校验结果不得声称完成。
