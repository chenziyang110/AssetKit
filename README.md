# AssetKit

面向 AI Agent 的项目资产管理 Skill：先检索再创建，按规范存放，一资产版本一卡片，通过工具增量登记和更新。

AssetKit separates reusable skill instructions from each project's asset ledger. JSON records are the metadata source of truth; SQLite is a rebuildable local search index.
